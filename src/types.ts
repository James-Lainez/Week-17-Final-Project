export type Music = {
    song: string
    genre: string
    artist: string
    price: number
    id: string
}

export type cart = {
    song: Music
    artist: string
    price: number
    id: string
}